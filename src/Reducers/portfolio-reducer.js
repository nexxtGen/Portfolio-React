

const initialState = {  
        
};

const portfolioReducer = function (state = initialState, action) {
    switch (action.type) {
        
    }
    return state;
};

export default portfolioReducer;